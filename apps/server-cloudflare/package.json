{
  "name": "@my-ip/server-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@my-ip/core": "workspace:*",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.8.2",
    "wrangler": "^3.111.0"
  }
}
